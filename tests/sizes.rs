#[test]
pub fn sizes() {
    // dbg!(size_of::<Tracer>());
    // dbg!(size_of::<Usage>());
    // dbg!(size_of::<ExpectTrack>());
    // dbg!(size_of::<SuggestTrack>());
    // dbg!(size_of::<EnterTrack>());
    // dbg!(size_of::<StepTrack>());
    // dbg!(size_of::<DebugTrack>());
    // dbg!(size_of::<OkTrack>());
    // dbg!(size_of::<ErrTrack>());
    // dbg!(size_of::<ExitTrack>());
    // dbg!(size_of::<Track>());
    // dbg!(size_of::<ParseOFError>());
    // dbg!(size_of::<OFCode>());
    // dbg!(size_of::<Suggest2>());
    // dbg!(size_of::<Expect2>());
    // dbg!(size_of::<DebugWidth>());
    // dbg!(size_of::<Span>());
}
